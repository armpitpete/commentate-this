---
authority_record: true
standard: Recursive Project Improvement Standard v1.0
---

# Project Authority

## Source authority

The repository source authority is:

- preserved Git history;
- `main` at exact commit `5fef5a4ebd443d0bd5b9cbb11c83b8d121fdcf9a`;
- the CT-01 product contract, fixed reference sentences and retained provider evidence on PR #1;
- PR #1 at exact head `b4cb8d6765327904e07522f887c2576ae50389cd` while it remains open and unmerged;
- `Recursive Project Improvement Standard v1.0`.

PR #3 supplies repository control only. It does not replace CT-01 product authority and does not authorise its own promotion.

## Active authority

`STATUS.md` is the sole repository-level completion authority.

The current bounded lane is the CT-01 provider-backed audio proof recorded in `STATUS.md`. Product contracts, pull requests, provider responses, generated manifests, listening pages and score sheets are supporting evidence and must not silently compete with `STATUS.md`.

## Decision authority

A substantial change requires a bounded contract recording:

- one observable goal;
- the exact starting repository and pull-request heads;
- allowed files and behaviours;
- forbidden files and behaviours;
- expected result;
- validation commands and evidence;
- promotion rule;
- stop rule.

Provider access and human listening judgements remain human-controlled gates. Unavailable external checks must be recorded as unavailable.

## Completion authority

Exactly one Markdown file may declare:

```yaml
completion_authority: true
```

That file must be root `STATUS.md`.

Completion of a provider call, generated script, rendered audio file, CI run, pull request or draft result does not begin another lane automatically.

## Governing constraints

- Repository evidence outranks chat memory.
- Reviewed and current heads must match before work or promotion.
- The CT-01 fixed ten-sentence proof set remains the current evidence boundary.
- The free ElevenLabs lane remains closed unless access materially changes and a new contract is authorised.
- Azure remains fallback evidence; Google Gemini TTS remains the immediate audition gate while PR #1 retains that authority.
- No named-person voice cloning or imitation is permitted.
- No crowd, interface, account, sharing or microphone expansion begins before the provider and voice proof is accepted.
- No new lane begins automatically after closure of the current lane.
